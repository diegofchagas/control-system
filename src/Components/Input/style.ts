import styled from "styled-components";

export const Container = styled.div`
  input,
  label {
    border: none;
    font-weight: 600;
    padding: 0.625rem 0.75rem;
    border-radius: 16px;
    color:#5996B7;
    cursor: pointer;
    font-size:.900rem;
  
  }
`;
